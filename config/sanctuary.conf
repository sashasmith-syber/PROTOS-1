# PROTOS-1 Sanctuary Allowlist
#
# This file defines which source identifiers are authorized to access the system.
# Each line represents one allowed source identifier.
#
# Format:
# - One identifier per line
# - Lines starting with # are comments
# - Empty lines are ignored
# - No wildcards or patterns (exact match only)
#
# SECURITY NOTES:
# - Use non-sensitive identifiers (node names, logical IDs)
# - Do NOT use IP addresses if they could be spoofed
# - Do NOT include secrets or credentials here
# - This file should be version controlled (it contains no secrets)
#
# Example entries:
# node-alpha
# node-beta
# agent-1
# local-dev-instance

# Default development nodes
localhost
local-dev
test-agent

# Add your authorized sources below:
